# 字节码压缩实现说明（旧 LZ77 方案已退役）

仓库早期使用过 LZ77/LZ4 风格 token 流；该路径已由 DEFLATE 替代，不再是当前产物格式。请勿根据旧的 K1/K2 明文头、`lz77d` 或压缩开关说明编写解析器。

## 当前固定流程

```text
每 prototype 独立 keys、CFG block-state 的 v3 序列化正文
→ DEFLATE
→ 流式 XOR
→ seed-bound integrity tag 与 version/features 头
→ basE91
```

固定 CLI 配置始终启用 DEFLATE，不提供压缩模式或强度选择。运行端先验证头和加密正文的完整性，再解密、inflate 和反序列化。

当前实现及格式细节见：

- [`DEFLATE压缩实现说明.md`](DEFLATE压缩实现说明.md)
- [`HARDENING_REPORT.md`](HARDENING_REPORT.md)
- `IronBrew2/Bytecode Library/Bytecode/Serializer.cs`
- `IronBrew2/Obfuscator/VM Generation/VMStrings.cs`

## 兼容性原则

- 以 Lua 5.1 语义差分为准，不以仅能成功生成文件作为正确性证明。
- 未知 format version 或保留 feature bits 必须立即拒绝。
- 完整性失败统一报 `invalid protected payload`。
- 小脚本是否值得携带 inflater 需要性能和体积基准；在完成基准前不增加新的用户档位。
