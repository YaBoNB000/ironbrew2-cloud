# 字节码压缩（LZ77）实现说明

## 核心思路

之前的 LZW 压缩**无效**，是因为压缩发生在**流式 XOR 加密之后**——加密后的字节码是高熵数据，
LZ77/LZW 都压不动（压缩率 ≈ 1.0x，甚至膨胀）。

正确顺序必须是：**明文字节码 → LZ77 压缩 → 再 XOR 加密 → base91**。

## 数据流（混淆端 → 运行端）

```
混淆端（C# Serializer）:
  序列化明文 body（opcode 用 K1/K2 逐条加密，其余明文）
  → LZ77 压缩（LZ4 兼容 token 流）
  → 流式 XOR 加密（种子 = 盐|环境指纹）
  → 拼 header：盐(4B) + K1(2B) + K2(2B) + 压缩标志(1B)
  → base91 编码进产物

运行端（VM 模板 VMP1）:
  base91 解码 → 读盐派生种子 → 读 K1/K2/flag
  → 整体 XOR 解密 body
  → 若 flag==1 则 LZ77 解压（lz77d）
  → 解析明文字节码
```

## 改动文件

| 文件 | 改动 |
|---|---|
| `IronBrew2/Bytecode Library/Bytecode/Lz77.cs` | **新增**：LZ4 兼容 LZ77 压缩器（greedy + 3 字节哈希表） |
| `IronBrew2/Bytecode Library/Bytecode/Serializer.cs` | 重构：明文序列化(SerializeBody) → LZ77 → XOR → 拼 9 字节头（含 flag） |
| `IronBrew2/Obfuscator/VM Generation/VMStrings.cs` | VMP1：加 lz77d 解压器 + 整体 XOR 解密 + 按 flag 条件解压；gBits*/gString 去逐字节 XOR |
| `IronBrew2/Obfuscator/VM Generation/Generator.cs` | 数据段统一 base91；base91 解码器总是生成 |

## 调试过程中发现并修复的 bug

1. **LZ4 offset 顺序错误**（关键）：LZ4 格式里 offset 必须在 match 长度扩展**之前**，
   最初实现写反了，导致 m≥15 的匹配解压错位（dist 读出错误值）。
   随机数据 round-trip 测试没暴露（随机数据几乎无 ≥19 字节匹配），
   真实字节码（大量重复结构）触发。已修复并验证。

2. **无压缩路径的 flag**：最初 `BytecodeCompress=false` 时 VM 端仍执行 lz77d 解压，
   会把明文当压缩数据解坏。增加 header 第 9 字节 flag，VM 端按 flag 决定是否解压。

## 体积效果（实测）

| 脚本 | 有压缩 | 无压缩 | 差异 |
|---|---|---|---|
| bench（118B 小脚本） | 56.4 KB | 51.2 KB | 压缩反而 +10%（解压器固定开销 > 节省） |
| strheavy（1.5KB，40 字符串） | 142 KB | 152 KB | **-6.7%** |
| big（10KB，60 函数） | 80.5 KB | 87.2 KB | **-7.7%** |

**结论**：字节码越大（尤其 EncryptStrings 展开后），压缩收益越明显。
小脚本因 lz77d 解压器 + 整体 XOR 循环的固定开销，压缩反而略大。

## 功能验证（全部实测通过）

| 测试 | 结果 |
|---|---|
| low 纯 Lua（无防护档） | ✅ 输出 30 |
| high 纯 Lua（环境绑定） | ✅ 拦截（乱码报错） |
| 执行器环境 bench/strheavy/big | ✅ 输出与原始脚本逐字节一致 |
| 无压缩（flag=0）路径 | ✅ 正常 |
| 反 dump loadstring hook | ✅ 拦截 dump 词、放行正常脚本 |

## 建议

- 默认保持 `BytecodeCompress=true`（大脚本收益明显）
- 若主要混淆小脚本且在意体积，可考虑按脚本大小自动开关压缩（比如明文字节码 > 20KB 才启用），
  需要的话可以加这个自适应逻辑
