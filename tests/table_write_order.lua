local values = {
    alpha = 1,
    beta = 2,
    gamma = 3,
    delta = 4,
    epsilon = 5,
}
local test_output = values.alpha .. ":" .. values.beta .. ":" .. values.gamma .. ":" .. values.delta .. ":" .. values.epsilon
print(test_output)
return {__ib2_test_output = test_output}
