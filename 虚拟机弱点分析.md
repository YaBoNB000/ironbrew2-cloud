# Lua/Luau 虚拟机混淆器弱点分析

基于你发来的几版样本，包括：

```text
test_obf.txt
test_obf (9).txt
```

我从解包、VM prototype 解析、常量恢复、指令 trace 的角度看，你这个虚拟机壳已经比普通字符串加密/控制流混淆强不少，但仍然有一些比较明显的弱点。

---

## 1. 总体评价

你的混淆器主要由这些层组成：

```text
短字符串表加密
→ 大块 Base91 类编码
→ 自定义归档格式
→ PRNG XOR 流加密
→ raw deflate 压缩
→ 自定义 VM prototype
→ VM 指令解释器
→ 环境检测/完整性检测
```

这套结构对普通脚本小子、普通 beautifier、简单字符串搜索是有效的。

但是对于有经验的逆向者来说，弱点也比较集中：

```text
所有解密逻辑都在客户端
所有 key 都能从脚本中派生
VM 格式比较稳定
解释器边界明显
常量最终必须变成明文
payload 语义最终必须被 VM 执行
```

所以它的核心问题是：

> 壳复杂，但不是不可逆；只要静态复刻解码器或插桩 VM，就能逐层剥开。

---

## 2. 最大弱点：解密器和密钥都在客户端

这是所有 Lua/Luau 客户端混淆器的根本问题。

你的脚本里包含了：

```text
Base91 解码器
归档解析器
PRNG 解密逻辑
deflate 解压器
VM prototype 解析器
常量解密逻辑
VM 指令解释器
```

也就是说，攻击者不需要猜算法。

只要读代码，就能复刻：

```text
输入混淆脚本
→ 调用同样的解密算法
→ 得到 VM prototype
→ 得到常量
→ 得到语义
```

这一点是最核心的弱点。

### 风险等级

```text
高
```

### 原因

你所有保护都在同一个 Lua 文件里完成。只要脚本能在用户端运行，逆向者也能让它在自己的环境里运行或重实现。

---

## 3. Base91 层比较容易识别

你用了类似 Base91 的变长编码：

```lua
value = byte - 33
if byte > 39 then value = value - 1 end
if byte > 92 then value = value - 1 end

if value >= 0 and value <= 90 then
    ...
end
```

再根据：

```lua
if (pair % 8192) > 88 then
    bits += 13
else
    bits += 14
end
```

输出字节。

这个特征非常明显。

即使你换变量名，逆向者也很容易搜到：

```text
8192
88
91
13
14
33
39
92
```

这些常量组合很像 Base91 变种。

### 风险等级

```text
中高
```

### 问题

这层主要只能防止直接看 payload，无法防止静态复刻。

### 建议

可以考虑：

```text
每次构建随机化编码 alphabet
随机化阈值和状态机
拆散解码器
把解码过程融合到 VM loader
不要保留明显的 Base91 常量组合
```

但注意，这只能增加成本，不能真正防止还原。

---

## 4. PRNG XOR 加密比较弱

你几版都使用了类似 LCG 的 PRNG：

```lua
key = key * A + B mod 2^32
byte = encrypted_byte ~ high_byte(key)
```

例如新版中出现过：

```text
642125 / 90544241
129509 / 450428767
```

这种加密对混淆来说够用，但从逆向角度很弱。

原因：

1. 算法简单；
2. 状态只有 32 bit；
3. 常量直接写在脚本里；
4. key 派生逻辑也在脚本里；
5. payload 格式有固定头部，容易验证是否解对。

### 风险等级

```text
高
```

### 攻击方式

逆向者不需要爆破，只需要复刻：

```python
byte = encrypted_byte ^ (key >> 24)
key = (key * A + B) & 0xffffffff
```

然后根据 header / checksum / deflate 是否成功判断。

### 建议

如果只是脚本混淆，可以换更复杂的流状态机，但不要指望真正安全。

更有效的是：

```text
让关键逻辑不在客户端
把真正敏感逻辑放服务端
客户端只保留最小 loader
```

---

## 5. 归档格式虽然变了，但结构仍然稳定

你几版里确实改过 block 表格式。

例如某版是：

```text
type: 1 byte
index: 2 bytes
length: 3 bytes
```

新版变成：

```text
length: 4 bytes
index : 2 bytes
type  : 1 byte
```

再新版又变成：

```text
type  : 1 byte
length: 4 bytes
index : 4 bytes
```

这些变化能打断旧脚本，但不能阻止人工分析。

原因是归档解析器仍然在 Lua 代码里，逆向者可以直接照着写。

### 风险等级

```text
中
```

### 弱点

虽然字段位置变了，但整体结构仍然是：

```text
header
metadata
block table
code block
data block
```

这个模式比较明显。

### 建议

可以考虑：

```text
让 block metadata 分散存储
不要集中出现完整 block 表
把部分字段放进 VM 指令流里动态恢复
让 code/data block 混排并交叉依赖
```

但这会明显增加加载复杂度。

---

## 6. raw deflate 是明显特征

你使用了 raw deflate 解压，脚本里包含完整 inflate 实现。

这类代码特征非常明显：

```text
固定 Huffman 表
长度表 3,4,5,6,7,8,9,10,11,13...
距离表 1,2,3,4,5,7,9,13...
256 end-of-block
```

逆向者看到这些表，基本就知道：

```text
前面某段数据最终会被 inflate
```

### 风险等级

```text
中
```

### 问题

deflate 只压缩，不加密。

一旦前面的 XOR 层解开，deflate 会帮逆向者验证结果是否正确。

### 建议

可以：

```text
不用通用压缩格式
或在压缩流前后插入自定义扰动
或把压缩解码融合进 VM 指令读取过程
```

但注意，这仍然只是增加工作量。

---

## 7. VM 解释器边界太清楚

你的脚本后半段有一个大型 VM loop。

结构大概是：

```lua
while true do
    integrity_check()
    pc = fix_pc(proto, pc, state)
    operands, opcode = decode_instruction(proto, pc, state, true)

    if opcode == nil then
        opcode = opcode_table[...]
    end

    -- dispatch
    ...

    pc = pc + 1
end
```

这个边界很适合插桩。

逆向者只需要在：

```lua
operands, opcode = decode_instruction(...)
```

之后插入：

```lua
TRACE(pc, opcode, operands, registers)
```

就可以让 VM 自己帮忙解密指令。

### 风险等级

```text
高
```

### 为什么这是弱点

即使静态反编译 opcode 很麻烦，动态 trace 也能绕过大量工作。

你的 VM 必须在执行前得到：

```text
当前 pc
当前 opcode
当前 operands
当前寄存器
```

这些都是非常好的观测点。

### 建议

可以增加难度：

```text
不要集中 decode 完一条完整指令
把 operand 解密分散到每个 opcode handler 内
让 opcode handler 自己按需解 operand
让 pc 更新、寄存器读写和常量解密交叉进行
减少统一 P(...) / decode_instruction(...) 边界
```

但代价是 VM 更复杂、性能更差、bug 更多。

---

## 8. 常量最终必须明文出现

你新版对字符串常量做了增强：

```text
分片重排
rolling key
按全局 position 更新 key
```

这比前几版更强。

但常量最终仍然要变成：

```lua
"pcall"
"getgenv"
"SCRIPT_KEY"
"loadstring"
"HttpGet"
URL
```

只要 VM 运行到使用常量的位置，常量一定会在内存中出现。

静态上，也可以复刻常量解密器。

### 风险等级

```text
高
```

### 本次样本中的例子

`test_obf (9).txt` 里最终只恢复出：

```text
9918 -> "pcall"
```

说明这个样本 payload 非常小。

之前新版中可恢复出：

```text
getgenv
SCRIPT_KEY
WLK-AF5W-ZOKZ-F6ZB-P4B3
loadstring
game
HttpGet
https://luavon.w0n.cn/api/v1/scripts/1/load
```

### 建议

如果想增加难度：

```text
不要一次性解完整字符串
按字符/片段按需解
使用后立即清理
把字符串拆成多个语义无关的片段
让片段来自不同执行路径
```

但本质上仍然会被 trace 捕获。

---

## 9. 环境检测容易 patch

你有环境检测，例如：

```lua
debug.getinfo
getconstants
getproto
getprotos
newcclosure
islclosure
iscclosure
checkcaller
Instance
Vector3
task
game:GetService("Players")
```

这些检测能挡掉一部分低级 sandbox。

但有经验的人可以：

```text
patch 检测返回值
mock executor API
跳过 anti-check 分支
直接复刻解包逻辑
```

例如几版里都有类似：

```lua
local ok = h1()
local magic = 3594637806
if ok and condition then
    ok, magic = check()
end
if ok and magic ~= 3594637806 then
    ok = false
end
```

这种结构很容易被替换成：

```lua
local ok = true
local magic = 3594637806
```

### 风险等级

```text
中高
```

### 建议

更强的做法是：

```text
不要让检测只影响一个布尔值
让检测结果参与 key 派生
让错误环境下解出的内容看起来合理但语义错误
把检测结果分散进多个常量/指令解密过程
```

但注意，如果检测算法也在客户端，仍然能被复刻或 patch。

---

## 10. opcode dispatch 可以逐步映射

你的 dispatch 虽然写得很乱，例如大量：

```lua
if state == 123 then
    ...
elseif state == 456 then
    ...
end
```

但最终 VM 操作类型有限：

```text
LOADK
GETGLOBAL
GETTABLE
SETTABLE
CALL
RETURN
CLOSURE
MOVE
NEWTABLE
```

只要多拿几个样本，opcode 很快能建立映射。

### 风险等级

```text
中高
```

### 问题

VM 指令集如果长期稳定，逆向者可以写通用反编译器。

### 建议

```text
每次构建随机 opcode 编号
每次构建随机 operand 布局
每次构建随机寄存器读写顺序
每次构建随机 handler 拆分/合并
不同函数使用不同 opcode map
```

你现在已经有一部分随机化，但核心 VM 结构仍然相似。

---

## 11. 多版本改动主要是常量换皮

你这几版确实改了不少：

```text
Base91 段数变了
PRNG 常量变了
block 表格式变了
code block 长度位置变了
字符串常量分片方式变了
VM chunk 数变了
```

这些改动能打断固定脚本。

但是整体模式没变：

```text
Base91
→ archive
→ LCG XOR
→ deflate
→ VM prototype
→ VM interpreter
```

所以人工分析时，只需要重新定位几个常量和字段。

### 风险等级

```text
中
```

### 建议

如果想让自动化更难：

```text
让每个样本使用不同架构，而不只是不同常量
随机选择是否压缩
随机选择压缩算法
随机选择 block 表结构
随机选择 VM prototype 布局
随机选择字符串常量编码器
```

---

## 12. payload 很小时，VM 反而暴露结构

`test_obf (9).txt` 最终只有：

```lua
pcall(function()
end)
```

这种 payload 太小，反而便于逆向者验证 VM 结构。

因为：

```text
VM prototype 小
chunk 少
常量少
指令少
语义容易猜
```

### 风险等级

```text
中
```

### 建议

测试样本可以加一些干扰逻辑，例如：

```text
假常量
假分支
死代码
无效闭包
混淆表构造
假 URL
假 loadstring
```

否则逆向者很容易用小样本校准反编译器。

---

## 13. 远程 loader 语义容易暴露

前一版样本最终是：

```lua
getgenv().SCRIPT_KEY = "WLK-AF5W-ZOKZ-F6ZB-P4B3"
loadstring(game:HttpGet("https://luavon.w0n.cn/api/v1/scripts/1/load"))()
```

这种 loader 一旦常量恢复，语义非常明显。

### 风险等级

```text
高
```

### 建议

如果你想保护真实业务逻辑，不应该把关键 URL、key、loader 逻辑完整放客户端。

更合理：

```text
服务端鉴权
短期 token
一次性 URL
请求参数签名
服务端按设备/会话返回不同内容
```

但仍然要记住：客户端最终拿到什么，就能被别人看到什么。

---

## 14. 你的 VM 当前最弱的点排序

按实际逆向影响排序：

| 排名 | 弱点 | 严重度 |
|---:|---|---|
| 1 | 解密器和 key 全在客户端 | 高 |
| 2 | VM 指令 decode 边界明显，方便插桩 | 高 |
| 3 | 常量最终会明文出现 | 高 |
| 4 | LCG XOR 流加密可直接复刻 | 高 |
| 5 | anti-check 可 patch | 中高 |
| 6 | VM 架构跨版本相似 | 中高 |
| 7 | Base91 特征明显 | 中高 |
| 8 | deflate 特征明显 | 中 |
| 9 | 小 payload 暴露 VM 结构 | 中 |

---

## 15. 优先改进建议

如果你想让下一版更难解，可以优先改这些。

### 15.1 减少统一指令解码点

现在类似：

```lua
operands, opcode = decode_instruction(proto, pc, state)
```

建议改成：

```text
handler 内部按需解 operand
不同 opcode 使用不同解码方式
常量解密和寄存器读取混在 handler 内
```

这样插桩难度会增加。

---

### 15.2 每个函数使用不同 VM 配置

包括：

```text
不同 opcode map
不同 operand layout
不同寄存器偏移
不同常量加密 seed
不同 block 顺序
```

避免写一次反编译器适配所有函数。

---

### 15.3 检测结果参与真实解密

不要只做：

```lua
if not ok then error() end
```

而是让检测结果影响：

```text
常量 key
opcode key
block key
pc transform
```

并且错误结果不要立刻崩溃，而是解出错误但看似合理的代码。

---

### 15.4 字符串按需解密

不要一次性还原完整字符串。

比如 URL 不要直接变成完整：

```lua
"https://example.com/path"
```

可以分散成：

```text
协议片段
域名片段
路径片段
参数片段
运行时拼接
使用后销毁
```

但是这只能增加 trace 成本，不能绝对防止。

---

## 16. 现实结论

你的 VM 对普通人来说已经够复杂：

```text
普通 beautifier 解不开
普通字符串搜索看不到 payload
简单 dump 不一定拿到原始逻辑
```

但对专门分析的人来说，它仍然存在核心弱点：

```text
算法在客户端
key 在客户端
VM 在客户端
payload 最终必须执行
```

所以它能提高逆向成本，但不能阻止逆向。

最实际的目标应该是：

```text
让自动化脚本失效
让通用反混淆器成本变高
让人工分析耗时变长
```
