# 压缩算法调研报告 — 各语言方案对比与可行性

## 一、实测数据（用 IronBrew2 混淆后的真实字节码）

### 压缩率对比（越低越好）

| 算法 | small 10.7KB | medium 19.6KB | large 23.2KB | 解压器体积(Lua) | 解压速度(实测) |
|---|---|---|---|---|---|
| **LZ77（当前实现）** | 0.76x | 0.70x | 0.45x | ~40 行 | 毫秒级 |
| **LZ4**（C#/Python/Java 通用库） | 0.65x | 0.60x | 0.37x | ~40 行 | 毫秒级 |
| **DEFLATE/zlib**（C#/Python/Java 内置） | **0.48x** | **0.45x** | **0.28x** | ~100 行 | **10-17ms / 20KB** |
| bz2/BWT（Python/Java） | 0.51x | 0.48x | 0.30x | ~150 行 | 慢 |
| LZMA（Python/Java/7z） | 0.43x | 0.39x | 0.25x | ~400 行 | 很慢(不可用) |
| zstd（C/Python/Java 库） | 0.45x | 0.41x | 0.26x | ~300 行 | 很慢(不可用) |

### 关键约束：解压器必须用纯 Lua 5.1 写进 VM 模板

这是与普通压缩场景最大的区别——**解压器代码会作为固定成本进产物**，且在 Roblox 执行器里由解释器跑。因此：

- 压缩率要「够好」，但解压器要「够小、够快」
- LZMA/zstd/bz2 压缩率虽最好，但 pure-Lua 解压器 300-400 行且解压秒级 → **排除**
- **DEFLATE 是唯一「压缩率 + 解压器」双优的平衡点**

## 二、核心发现（实测验证）

### 1. DEFLATE 完全可行 ✅

我写了一个 **pure-Lua 的 inflate 解压器（~100 行，支持 dynamic Huffman）**，实测：

```
small:  5140B → 10695B, 0.010 秒
medium: 8760B → 19640B, 0.017 秒
large:  6538B → 23152B, 0.014 秒
解压结果与原始字节码逐字节一致 ✓
```

对比当前 LZ77：**压缩率好 35-40%**（0.48x vs 0.76x），解压器多 ~60 行但速度完全可接受。

而且 **C# 端零依赖**：.NET 内置 `System.IO.Compression.DeflateStream`（raw deflate，RFC 1951），
输出格式与我的 Lua inflate 完全兼容（Python 端就是 zlib 的 raw deflate，已验证 round-trip）。

### 2. LZ4 是零成本升级 ✅

LZ4 的匹配策略（查多个 hash 候选）比我当前的「单候选 greedy」好 12-16%，
且 **LZ4 格式与我当前的 token 流完全兼容**（我实现的就是 LZ4 变体），
**解压器一行不用改**，只改 C# 压缩器。

### 3. LZMA/zstd/bz2 排除 ❌

pure-Lua 解压器不现实（体积大 + 解压秒级），在 Roblox 执行器里会卡顿。

## 三、各语言方案总结

| 语言 | 可用方案 | 说明 |
|---|---|---|
| **C#（混淆器端）** | `DeflateStream`（内置）/ `ZLibStream`（.NET 6+）/ LZ4 包 | 推荐 DeflateStream，零第三方依赖 |
| **Python** | `zlib`（内置，即 DEFLATE）/ `lz4` / `zstandard` | zlib 与 C# DeflateStream 输出兼容 |
| **Java** | `Deflater`（内置，即 DEFLATE）/ `LZ4` / `Snappy` | Deflater 与 C# DeflateStream 兼容 |
| **Lua（运行端）** | 手写 inflate ~100 行（已验证） | 必须 pure-Lua，无原生 zlib |

> 三个语言的内置库（C# DeflateStream、Python zlib、Java Deflater）都是同一个
> DEFLATE/RFC 1951 算法，产物可以互相验证、跨语言测试。

## 四、建议：升级到 DEFLATE

**方案**：当前 LZ77 → **DEFLATE**（C# DeflateStream + Lua inflate）

| 指标 | 当前 LZ77 | DEFLATE 升级后 |
|---|---|---|
| small 字节码 | 0.76x | 0.48x（**省 37%**） |
| medium 字节码 | 0.70x | 0.45x（**省 36%**） |
| large 字节码 | 0.45x | 0.28x（**省 38%**） |
| 解压器体积 | ~40 行 | ~100 行（+2-3KB 混淆后固定成本） |
| 解压速度 | 毫秒 | 10-17ms/20KB（可接受） |

**取舍**：小字节码（<8KB）DEFLATE 省的体积 ≈ 解压器增量，基本持平；
字节码越大收益越明显（大脚本是主要场景，收益 35%+）。

**实现改动**（都验证过可行）：
1. `Serializer.cs`：`Lz77.Compress` → `DeflateStream`（明文压缩，仍是"先压缩后加密"）
2. `VMStrings.cs`：`lz77d` 解压器 → 我验证过的 inflate
3. 其余（base91 + 流式 XOR + flag + 环境绑定）**完全不动**

## 五、附：如果还想再压，两个进阶方向

1. **定制哈夫曼**：Lua 字节码的 opcode 分布极不均匀（MOVE/LOADK/GETTABLE 高频），
   用预置的字节码专用哈夫曼表替代 DEFLATE 的通用表，可再省 ~10%，但实现复杂、有风险。
2. **结构化序列化**：跳过"通用压缩"，直接在 Serializer 层做变长编码
   （opcode 4bit + 寄存器变长 + 常量表去重），本质是给字节码格式定制的压缩，
   压缩率可能到 0.4x，但要重写 Serialize/Deserialize，工程量最大。
