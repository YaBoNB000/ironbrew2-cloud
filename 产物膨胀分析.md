# IronBrew2 产物膨胀分析

## 实测数据（118 字节源文件 → 各档产物）

| 配置 | 产物大小 | 膨胀倍数 |
|---|---|---|
| 源文件 | 118 B | 1× |
| low | 4.9 KB | 41× |
| mid（--no-antidump） | 11.2 KB | 95× |
| mid（Noise 关） | 108 KB | 917× |
| **mid（默认）** | **130 KB** | **1100×** |
| high（默认） | 130 KB | 1102× |

> 注：mid 和 high 对这个小脚本几乎一样大——因为 VM 解释器和 AntiDump 是**固定成本**，
> 跟脚本大小无关。脚本越大，膨胀倍数反而越低（固定成本被摊薄）。

## 膨胀账本（mid 默认 130KB 的构成）

```
130 KB 总产物
├── ~11 KB  VM 解释器（31 个 opcode handler + 解压器 + 数据结构）  ← 固定成本
├── ~119 KB AntiDump 防御块  ★★★ 膨胀之王，占 91%
│     ├── ~36 KB  数据段（defense/guard 字节码经 LZW+base92）
│     └── ~83 KB  新增的 116 个 opcode handler（防御块引入的新指令）
└── ~22 KB  Noise 噪声（每个 handler 三层膨胀）  ★★ 次因，占 16%
```

## 三大膨胀源头的原理

### ① AntiDump 防御块（+119KB，最主要）

你仓库里的 `DefenseGenerator` 生成了一段 **7KB 的反 dump 防御代码**，包含：
- 80000 元素内存膨胀 + 死循环的「冻结函数」
- 反 dump 关键词黑名单（getscriptbytecode/decompile/dumpstring 等 25 个，全 `string.char` 编码）
- `loadstring`/`writefile`/`readfile`/`listfiles`/`appendfile` 的 hook 包装
- `debug.getinfo/getupvalue/getconstants/getprotos/getregistry` 扫描 + `task.spawn` 定时巡检

这段代码编译成 **17KB 字节码**，合并进主 chunk 后产生**双重膨胀**：

1. **数据段 +36KB**：17KB 字节码 → LZW 压缩（对 XOR 加密后的高熵字节码**压不动甚至膨胀**）→ base92 编码再 +30% → 37KB
2. **handler +83KB**：防御块用了主脚本没用到的 ~116 个 opcode，**每个都要生成完整 handler**，再叠加 Noise 膨胀到 ~1KB/个

### ② Noise 噪声（+22KB）

每个 opcode handler 被套**三层**（`Generator.cs` 的 FlattenCode + AntiDump 的 HandlerNoise）：

```lua
-- 原本一行: pe8[A] = pe8[B] + pe8[C]
-- 膨胀后:
local t7521=0;repeat t7521=t7521+1;if t7521==1 then
  local _m2474;local _m1540;local _m4857;...  -- 一堆空变量(HandlerNoise)
  pe8[g09[(1*2)]] = pe8[g09[(4-0)]] + pe8[g09[(4-1)]];  -- 数字也打散成 (1*2)
end;until t7521>=1;
```

实测关掉 Noise：130KB → 108KB（省 16%）。

### ③ LZW + base92 数据段编码（对加密数据无效）

`Generator.Compress` 用 **LZW** 压缩字节码再 base92 编码。但你的字节码已经过了**流式 XOR 加密**（`Serializer`），加密后熵接近随机，**LZW 压缩率为负**（越压越大），base92 还要 +30%。结果 19KB 字节码变成 37KB。

## 优化建议（按性价比排序）

### 方案 A：零成本 —— 用已有开关（立即可用）

CLI 已支持 `--no-antidump`。130KB → 11KB（省 92%）。

可在 `obfuscate.bat` 里加一个「轻量模式」关键词，或者在 low 档的基础上保留字符串加密。

### 方案 B：AntiDump 拆两档（改 DefenseGenerator）

把防御块拆成：
- **轻量版**（~2KB）：只留 guard 执行器特征检测（`getgenv/identifyexecutor` 判断），
  防「纯 Lua 模拟器跑」够用了
- **完整版**（现有 17KB）：hook 扫描 + debug 巡检，防「执行器里 dump」

产物大小可预期降到 ~30KB 级别，同时保留核心防护。

### 方案 C：数据段编码换掉 LZW（改 Generator.Compress）

对「已 XOR 加密」的字节码，LZW 无意义。改成：
- 直接 **base85/base92**（省掉 LZW 解压器代码 ~2KB，数据段 -40%）
- 或对**未加密**部分用 LZW（如果想保留压缩），加密后再 base92

### 方案 D：Noise 分级（改 Generator.FlattenCode）

现在**每个 handler**都三层膨胀。改成：
- 只对高频 opcode（LoadK/GetTable/SetTable/Call/Move/Jmp 等）加完整 Noise
- 其余只加 HandlerNoise、跳过 FlattenCode 状态机

预计 Noise 贡献从 22KB 降到 ~8KB。

### 方案 E：根治 —— handler 去重 + 变体复用

mutation/super operator 会生成大量**语义重复**的 handler。可以在生成时做
「等价 handler 去重」，多个 VIndex 共用一个 handler 实现，只保留分支映射。
这能把 handler 数量砍掉一半以上。

## 结论

1. **膨胀主因是 AntiDump（91%），不是 VM 本身**。VM 解释器只有 ~11KB。
2. 其次 Noise（16%）。
3. LZW+base92 对加密数据是负优化。
4. 最快见效：`--no-antidump`；最平衡：方案 B（AntiDump 拆档）+ 方案 C（换编码），
   可把 130KB 压到 ~30KB 且基本不损防护。

> 所有数字基于 bench.lua（118 字节）实测。大脚本的膨胀倍数会低很多，
> 因为 VM 解释器和 AntiDump 是固定成本；但**绝对值**上，AntiDump 的
> 119KB 和 Noise 的 22KB 对任何脚本都是同样的负担。
