# IronBrew2 产物膨胀分析

> 旧版多配置数据已经失效：当前 CLI 只有一套固定配置，且 serializer、完整性头、prototype keys、常量编码与 VM 模板均已改变。旧数据不能用于估算当前产物。

## 当前固定配置的主要体积来源

1. **认证高熵 envelope**：固定配置每次由 CSPRNG 新增 64–96 KiB entropy，并加入 record framing；这是当前小脚本最大的固定 payload 成本。basE91 约 1.23 倍展开后，仅随机区对应的文本通常约增加 80.6–121 KiB。
2. **VM、AntiDump 与 opcode handler**：解释器、capability guard 和 invocation-local block cache 路径属于固定配置；只生成实际需要的 handler 可限制其中一部分增长。
3. **pure-Lua inflate、envelope 恢复与 basE91 解码器**：固定配置启用 DEFLATE 和 authenticated envelope，因此每个产物都携带恢复逻辑。
4. **真实序列化正文**：DEFLATE 在 entropy envelope 与外层 XOR 前执行，避免对高熵数据做无效压缩；真实流另有由全量 entropy digest 派生的内层 state mask。
5. **控制流与 VM 多态代码**：名称、schema 顺序、handler 结构，以及 v3 每块 verifier/integrity/successor/state framing 会增加一定源码和 payload 体积。
6. **可选实验防御**：EnvironmentLock、AggressiveDefense、Noise、Mutation 和 SuperOperator 不属于固定 CLI 配置；AntiDump 已固定开启。

## 已采用的控制措施

- 固定关闭高误判的执行器 API hook、EnvironmentLock、AggressiveDefense 和噪声层；固定开启经过 capability/provenance/canary 筛选的 staged AntiDump。
- 固定关闭容易放大闭包数量的源码字符串解密函数；字符串常量改由 v3 serializer 的外层和内层编码保护。
- 仅对真实正文使用 DEFLATE；压缩发生在 entropy envelope 与加密之前，不尝试压缩 CSPRNG 高熵 records。
- 64–96 KiB 随机区是用户选择的固定防裁剪/形态成本，不因输入很小而降档。
- VM 仅包含输入实际需要的 opcode handler。

## 尚需建立的基准

应在相同提交、相同 Lua 5.1 工具链下记录：

| 输入类别 | 建议指标 |
|---|---|
| 极小脚本 | 最终字节数、固定 VM/解码器占比 |
| 字符串密集脚本 | 原始/序列化/DEFLATE/basE91 各阶段大小 |
| 多函数与深层闭包 | prototype 数、handler 数、生成与运行时间 |
| 大型真实脚本 | 峰值内存、首次运行延迟、最终膨胀倍数 |

每个样本至少进行多次随机构建，分别报告均值、范围、envelope 原始/文本增量、生成/恢复时间和语义差分结果。当前自动 verifier 已逐次检查 entropy 规模为 64–96 KiB、Shannon entropy ≥ 7.95 bits/byte、完整恢复和跨生成独立性；完整的多类输入性能基准仍待建立。完成这些基准前，不根据旧版数字新增“轻量/完整”档位。

## 结论

当前优先级是固定配置的语义稳定性和 v4 payload/CFG-state 防护；64–96 KiB authenticated/state-coupled envelope 是明确接受的固定体积取舍，不用额外档位隐藏。后续若实现自适应压缩，只能优化真实正文，不能擅自降低固定 entropy 规模，并应保持 CLI 行为单一。
