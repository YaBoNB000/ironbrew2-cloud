# IronBrew2 产物膨胀分析

> 旧版多配置数据已经失效：当前 CLI 只有一套固定配置，且 serializer、完整性头、prototype keys、常量编码与 VM 模板均已改变。旧数据不能用于估算当前产物。

## 当前固定配置的主要体积来源

1. **VM 与 opcode handler**：解释器框架是小脚本最明显的固定成本；只生成实际需要的 handler 可限制这部分增长。
2. **pure-Lua inflate 与 basE91 解码器**：固定配置启用 DEFLATE，因此每个产物都携带恢复逻辑。
3. **加密后的序列化正文**：DEFLATE 在 XOR 前执行，避免对高熵密文做无效压缩；basE91 仍有文本表示开销。
4. **控制流与 VM 多态代码**：名称、schema 顺序及 handler 结构随机化会增加一定源码体积。
5. **可选实验防御**：AntiDump、AggressiveDefense、Noise、Mutation 和 SuperOperator 不属于固定 CLI 配置，因此不应计入默认体积。

## 已采用的控制措施

- 固定关闭高误判且有较大体积成本的执行器 API hook、环境 gate 和噪声层。
- 固定关闭容易放大闭包数量的源码字符串解密函数；字符串常量改由 v2 serializer 的外层和内层编码保护。
- 使用 DEFLATE 后再加密，而不是压缩 XOR 后的高熵数据。
- VM 仅包含输入实际需要的 opcode handler。

## 尚需建立的基准

应在相同提交、相同 Lua 5.1 工具链下记录：

| 输入类别 | 建议指标 |
|---|---|
| 极小脚本 | 最终字节数、固定 VM/解码器占比 |
| 字符串密集脚本 | 原始/序列化/DEFLATE/basE91 各阶段大小 |
| 多函数与深层闭包 | prototype 数、handler 数、生成与运行时间 |
| 大型真实脚本 | 峰值内存、首次运行延迟、最终膨胀倍数 |

每个样本至少进行多次随机构建，分别报告均值、范围和语义差分结果。完成这些基准前，不根据旧版数字新增“轻量/完整”档位。

## 结论

当前优先级是固定配置的语义稳定性和 v2 payload 防护，而不是用额外档位隐藏体积差异。后续若实现自适应压缩，应由输入大小和实测收益自动决定，并保持 CLI 行为单一。
